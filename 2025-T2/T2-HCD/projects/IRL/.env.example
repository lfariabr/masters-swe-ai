# Server Configuration
NODE_ENV=development
PORT=3000

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Redis Commander Configuration
REDIS_CMD_USER=
REDIS_CMD_PASSWORD=

# Rate Limiting Defaults
DEFAULT_RATE_LIMIT=100
DEFAULT_WINDOW_MS=60000

# DEFAULT_RATE_LIMIT and DEFAULT_WINDOW_MS look numeric, 
# but they’ll hit your code as strings. 
# When you wire up the actual rate limiter, 
# make sure you parse and validate them 
# (and fail fast on garbage) instead of letting a 
# NaN sneak into your algorithm.

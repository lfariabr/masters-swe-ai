# GitHub Issue List - ISY503 Assessment 3: NLP Sentiment Analysis

| # | Issue Name | Description |
|---|---|---|
| 1 | Project setup & structure | Create `src/`, `tests/`, `notebooks/`, `outputs/`, `embeddings/`, and `docs/`. Add `requirements.txt`, `.gitignore` (`outputs/`, `embeddings/`, `__pycache__`, `.env`, large checkpoints), and `README.md` with install, train, evaluate, and app commands. |
| 2 | Implement review parser (`parser.py`) | Write `parse_review_file()` using BeautifulSoup (`html.parser`) to extract `<review_text>`, `<rating>`, and useful metadata from each `.review` file. Write `load_all_domains()` to iterate all 4 domain folders and return `text`, `rating`, `label`, `domain`, and `source_file`. Primary label comes from the filename (`positive.review` / `negative.review`). |
| 3 | Label audit & preprocessing (`preprocess.py`) | Implement `audit_labels()` to flag 3-star reviews and rating/filename conflicts. Implement `clean_text()`, `remove_outliers()` with configurable min/max word counts, and `split_data()` using reproducible stratified 70/15/15 train/validation/test splits. Stratify by label, and preserve domain metadata for analysis. |
| 4 | EDA & feature analysis (`features.py` + `notebooks/EDA.ipynb`) | Compute class balance, domain balance, rating distribution, review length distribution, and label-audit counts. Save `outputs/length_distribution.png` and `outputs/domain_balance.png`. Use these outputs to justify outlier thresholds and ethics discussion. |
| 5 | Vocabulary builder (`dataset.py`) | Implement `build_vocab()` from training texts only to avoid leakage. Include `<pad>` and `<unk>` tokens, configurable vocabulary size/min frequency, and save vocabulary metadata for inference. |
| 6 | PyTorch Dataset & DataLoader (`dataset.py`) | Implement `tokenize_and_pad()` with `max_len=256`, `ReviewDataset`, and `make_dataloaders()`. Dataloaders should support batching, shuffling for training, fixed seed behavior, and reuse by `train.py` and `evaluate.py`. |
| 7 | Optional GloVe loader (`dataset.py`) | Implement `load_glove()` as an optional enhancement, not a required dependency. If `embeddings/glove.6B.100d.txt` exists, map vocab to pretrained vectors; otherwise train the BiLSTM with learned embeddings. Document this fallback in `README.md`. |
| 8 | TF-IDF + Logistic Regression baseline (`baseline.py`) | Build an sklearn `Pipeline` with `TfidfVectorizer(max_features=30_000, ngram_range=(1,2))` and `LogisticRegression`. Train on the training split, evaluate on validation/test splits, print metrics, and save `outputs/baseline.joblib`. |
| 9 | BiLSTM model definition (`model.py`) | Implement `BiLSTMSentiment` as `Embedding -> Dropout -> BiLSTM (2 layers, bidirectional) -> Linear`. Support learned embeddings by default and optional GloVe initialization. Output raw logits for `BCEWithLogitsLoss`. |
| 10 | Training loop (`train.py`) | Implement seeded training with Adam, `BCEWithLogitsLoss`, gradient clipping, validation accuracy/F1 tracking, and best-checkpoint saving to `outputs/bilstm.pt`. Log loss, accuracy, and F1 per epoch. |
| 11 | Evaluation & error analysis (`evaluate.py`) | Load baseline and BiLSTM artifacts, run both on the same test split, and output accuracy, precision, recall, F1, confusion matrix, and misclassified examples. Save `outputs/confusion_matrix.png` and `outputs/error_examples.csv`. |
| 12 | Inference module (`inference.py`) | Implement `predict_sentiment(text, model_name)` returning `{"label": "Positive review"|"Negative review", "confidence": float}`. Handle raw-string cleaning internally and support both baseline and BiLSTM artifacts. |
| 13 | Streamlit app (`app.py`) | Build the required web interface: text area, explicit classify button, model selector, and output text exactly in the form `Positive review` or `Negative review`. Use `st.cache_resource` for loading artifacts and show confidence without making it required for marking. |
| 14 | Unit tests (`tests/`) | Write pytest tests for parser extraction, missing-field handling, label audit, text cleaning, outlier removal, split reproducibility, vocabulary no-leakage, padding length, model forward-pass shape, and inference response shape. |
| 15 | Demo acceptance test cases (`docs/demo-test-cases.md`) | Create facilitator-style test inputs covering clear positive, clear negative, short ambiguous, domain-shifted, and outside-training examples. Record expected behavior and final observed model outputs for the presentation/demo. |
| 16 | Presentation pack (`docs/presentation-outline.md`) | Create a 10-15 minute group presentation outline with speaker allocation, rationale, dataset overview, architecture diagram, training pipeline, model comparison, demo flow, accuracy results, error analysis, ethics, limitations, and future work. |
| 17 | Individual contribution report scaffold (`docs/individual-report-template.md`) | Create a 250-word template with placeholders for student name, student ID, team member names/IDs, contribution percentages summing to 100%, personal contribution summary, team contribution summary, and APA-cited NLP ethics note. |
| 18 | Contribution log & team table (`docs/contribution-log.md`, `docs/team-contribution-table.md`) | Track who owns parser, preprocessing, model training, evaluation, app, presentation, and report artifacts. Include evidence fields for branches, commits, PRs, meetings, and final percentage contribution. |
| 19 | References & ethics notes (`docs/references.md`, `docs/ethics-notes.md`) | Collect APA references for dataset source, sentiment/NLP methods, bias/label noise, and AI ethics. Cover filename/rating label noise, 3-star ambiguity, domain bias, review sarcasm/mixed sentiment, and limits of binary sentiment classification. |
| 20 | Submission package & GitHub evidence (`docs/submission-checklist.md`) | Create final checklist for group code, GitHub repository link, README instructions, model artifacts, presentation/video owner, individual report, team IDs, academic integrity declaration, and retained backup copy. Include Git workflow evidence. |
| 21 | Optional DistilBERT stretch (`notebooks/distilbert.ipynb` or `src/transformer_model.py`) | Only after the core pipeline works, optionally fine-tune DistilBERT on the same train/validation/test split for comparison. Keep this separate from the required BiLSTM path and label results as stretch work. |

## Critical Path

Complete issues 1-16 and 20 for a complete group submission. Complete issues 17-19 for the individual contribution/report requirement. Issue 7 is optional at runtime because the BiLSTM must work without GloVe. Issue 21 is stretch work only.

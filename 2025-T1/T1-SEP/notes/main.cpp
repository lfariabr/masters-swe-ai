#include <iostream>

int main(int argc, const char * argv[]) {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}

// Compile with: g++ -o hello main.cpp
// Run with: ./hello

// C++ is a compiled language, so it needs to be compiled before it can be run.
// The g++ compiler is used to compile C++ code.
// For bigger projects, we can use make or CMake to manage the build process.